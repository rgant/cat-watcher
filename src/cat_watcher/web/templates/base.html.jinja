<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description"
          content="Cat Watcher: motion-clip ingestion, cat detection, and inactivity alerts for indoor litter-box cameras.">
    <title>
      {% block title %}
        Cat Watcher
      {% endblock title %}
    </title>
    <link rel="icon"
          type="image/svg+xml"
          href="{{ url_for('static', path='favicon.svg') }}">
    <link rel="stylesheet" href="{{ url_for('static', path='style.css') }}">
    <script src="https://unpkg.com/htmx.org@2.0.4"
            integrity="sha384-HGfztofotfshcF7+8n44JQL2oJmowVChPTg48S+jvZoztPfvwD79OC/LTtG6dMp+"
            crossorigin="anonymous"
            defer></script>
    <script src="{{ url_for('static', path='auto_refresh.js') }}" defer></script>
    {{ dev_hot_reload_script | safe }}
  </head>
  <body {% block body_attrs %}{% endblock body_attrs %}>
    {# Active-link highlight: per-link aria-current="page" rather than a JS-driven class so screen
    readers announce the current page even before any script runs. #}
    {% set current_path = request.url.path %}
    <nav class="primary" aria-label="Primary">
      <a href="{{ url_for('root') }}"
         {% if current_path == "/" or current_path.startswith("/timeline") %}aria-current="page"{% endif %}>Timeline</a>
      <a href="{{ url_for('list_clips') }}"
         {% if current_path.startswith("/clips") %}aria-current="page"{% endif %}>Clips</a>
      <a href="{{ url_for('cameras_page') }}"
         {% if current_path.startswith("/cameras") %}aria-current="page"{% endif %}>Cameras</a>
      <a href="{{ url_for('stats_page') }}"
         {% if current_path.startswith("/stats") %}aria-current="page"{% endif %}>Stats</a>
      <a href="{{ url_for('alerts_page') }}"
         {% if current_path.startswith("/alerts") %}aria-current="page"{% endif %}>Alerts</a>
    </nav>
    <main>
      {% block content %}
      {% endblock content %}
    </main>
  </body>
</html>
